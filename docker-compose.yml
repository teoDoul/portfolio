version: "3.8"

services:
  portfolio:
    image: nginx:latest
    container_name: portfolio
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - web
